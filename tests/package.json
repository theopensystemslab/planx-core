{
  "private": true,
  "scripts": {
    "test": "jest",
    "test:regression": "",
    "lint": "eslint './src/**/*.{js,ts}' && prettier -c ./src",
    "lint:fix": "eslint --fix './src/**/*.{js,ts}' && prettier -w ./src",
    "check": "tsc && pnpm lint"
  },
  "dependencies": {
    "@opensystemslab/planx-core": "link:..",
    "dotenv": "^16.3.1",
    "dotenv-expand": "^10.0.0",
    "jest": "^29.6.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.6.2",
    "eslint": "^8.45.0",
    "prettier": "^3.0.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  }
}
